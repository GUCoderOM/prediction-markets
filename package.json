{
  "name": "prediction-monorepo",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run install-all && npm run build:backend && npm run build:frontend",
    "build:backend": "npm run build --prefix backend || true",
    "build:frontend": "npm run build --prefix frontend",
    "start": "npx concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}